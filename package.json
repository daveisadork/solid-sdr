{
  "name": "with-solid",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev:web",
    "dev:server": "turbo run dev:server",
    "dev:app": "turbo run dev:app",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "release:build": "goreleaser build --snapshot --clean && rm -rf apps/bridge/internal/static/web",
    "release": "goreleaser release --clean && rm -rf apps/bridge/internal/static/web"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.8",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@10.18.3",
  "engines": {
    "node": ">=18"
  }
}
